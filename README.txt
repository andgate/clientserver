-- Running a server
The command to run the server is
server <rows> <colows>

Where rows and columns specify the size of the plane ticket table.

-- Client configuration
Client's must be run alongside a config.txt file.
The config file must contain the following fields, in order:
<ip address> <port number> <timeout>

Note: Timeout is in seconds.